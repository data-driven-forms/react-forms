/* TODO:
    - collect all schema from all demos
    - attention: some of them are slightly different (PF4 wizard vs all other wizards, etc.)
*/

const schemas = {
  wizardSchema: { fields: [] },
  wizardPF4Schema: {}
};

export default schemas;
