import { Requireable } from 'prop-types'

export const optionsPropType: { [key: string]: Requireable<any> }

export const meta: { [key: string]: Requireable<any> }

export const formGroup: { [key: string]: Requireable<any> }

export const input: { [key: string]: Requireable<any> }

export const childrenPropTypes: { [key: string]: Requireable<any> }
